import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Self-host fonts via next/font — no external font CDN requests
  // No analytics, no tracking pixels
}

export default nextConfig
